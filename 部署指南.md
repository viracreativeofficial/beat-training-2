# 完整部署指南：从 Git 到 Vercel

## 📋 目录
1. [准备工作](#准备工作)
2. [初始化 Git 仓库](#初始化-git-仓库)
3. [创建 GitHub 仓库](#创建-github-仓库)
4. [上传代码到 GitHub](#上传代码到-github)
5. [部署到 Vercel](#部署到-vercel)
6. [后续更新](#后续更新)

---

## 准备工作

### 1. 安装必要工具

确保你已经安装了以下工具：

- **Git**: [下载 Git](https://git-scm.com/downloads)
- **Node.js**: [下载 Node.js](https://nodejs.org/) (应该已经安装)
- **GitHub 账号**: [注册 GitHub](https://github.com/signup)
- **Vercel 账号**: [注册 Vercel](https://vercel.com/signup)

### 2. 验证安装

打开 PowerShell 或命令提示符，运行：

```bash
git --version
node --version
npm --version
```

---

## 初始化 Git 仓库

### 步骤 1: 打开项目目录

在 PowerShell 中，导航到你的项目目录：

```bash
cd c:\Users\pao81\beat-training-2
```

### 步骤 2: 初始化 Git 仓库

```bash
git init
```

### 步骤 3: 配置 Git 用户信息（如果还没配置）

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

### 步骤 4: 添加所有文件到暂存区

```bash
git add .
```

### 步骤 5: 创建第一次提交

```bash
git commit -m "Initial commit: Beat Training App"
```

---

## 创建 GitHub 仓库

### 步骤 1: 登录 GitHub

1. 访问 [github.com](https://github.com)
2. 登录你的账号

### 步骤 2: 创建新仓库

1. 点击右上角的 **"+"** 图标
2. 选择 **"New repository"**

### 步骤 3: 填写仓库信息

- **Repository name**: `beat-training-2` (或你喜欢的名字)
- **Description**: `Beat Training App - 节拍训练应用`
- **Visibility**: 
  - ✅ **Public** (公开，免费)
  - ⬜ **Private** (私有，需要付费账号)
- ⬜ **不要**勾选 "Add a README file" (我们已经有了)
- ⬜ **不要**勾选 "Add .gitignore" (我们已经有了)
- ⬜ **不要**勾选 "Choose a license"

### 步骤 4: 创建仓库

点击 **"Create repository"** 按钮

### 步骤 5: 复制仓库地址

创建后，GitHub 会显示仓库地址，类似：
- HTTPS: `https://github.com/你的用户名/beat-training-2.git`
- SSH: `git@github.com:你的用户名/beat-training-2.git`

**复制 HTTPS 地址**（更容易使用）

---

## 上传代码到 GitHub

### 步骤 1: 添加远程仓库

在 PowerShell 中运行（替换为你的实际仓库地址）：

```bash
git remote add origin https://github.com/你的用户名/beat-training-2.git
```

### 步骤 2: 验证远程仓库

```bash
git remote -v
```

应该显示你的仓库地址。

### 步骤 3: 重命名主分支（如果需要）

```bash
git branch -M main
```

### 步骤 4: 推送代码到 GitHub

```bash
git push -u origin main
```

**注意**: 
- 如果是第一次推送，GitHub 会要求你登录
- 你可能需要使用 **Personal Access Token** 而不是密码
- 如果遇到认证问题，请参考下面的"认证问题解决"

### 步骤 5: 验证上传

1. 刷新你的 GitHub 仓库页面
2. 你应该能看到所有文件已经上传

---

## 认证问题解决

如果 `git push` 时遇到认证问题：

### 方法 1: 使用 Personal Access Token (推荐)

1. 访问 GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 点击 "Generate new token (classic)"
3. 设置权限：勾选 `repo` 权限
4. 生成并复制 token
5. 推送时，密码处输入这个 token

### 方法 2: 使用 GitHub CLI

```bash
# 安装 GitHub CLI
winget install GitHub.cli

# 登录
gh auth login

# 然后正常推送
git push -u origin main
```

### 方法 3: 使用 SSH (高级)

1. 生成 SSH 密钥：
```bash
ssh-keygen -t ed25519 -C "你的邮箱@example.com"
```

2. 复制公钥：
```bash
cat ~/.ssh/id_ed25519.pub
```

3. 在 GitHub → Settings → SSH and GPG keys 中添加公钥

4. 使用 SSH 地址添加远程仓库：
```bash
git remote set-url origin git@github.com:你的用户名/beat-training-2.git
```

---

## 部署到 Vercel

### 方法一：通过 Vercel 网站（最简单）

#### 步骤 1: 登录 Vercel

1. 访问 [vercel.com](https://vercel.com)
2. 点击 **"Sign Up"** 或 **"Log In"**
3. 选择 **"Continue with GitHub"** (推荐，这样可以直接访问你的仓库)

#### 步骤 2: 导入项目

1. 登录后，点击 **"Add New..."** 按钮
2. 选择 **"Project"**
3. 在仓库列表中找到 `beat-training-2`（或你的仓库名）
4. 点击 **"Import"**

#### 步骤 3: 配置项目

Vercel 会自动检测 Next.js 项目，通常默认设置即可：

- **Project Name**: `beat-training-2` (可以修改)
- **Framework Preset**: `Next.js` ✅
- **Root Directory**: `./` ✅
- **Build Command**: `npm run build` ✅
- **Output Directory**: `.next` ✅
- **Install Command**: `npm install` ✅

#### 步骤 4: 环境变量（如果需要）

如果你的项目需要环境变量：
1. 在 "Environment Variables" 部分添加
2. 格式：`变量名 = 值`

#### 步骤 5: 部署

1. 点击 **"Deploy"** 按钮
2. 等待构建完成（通常 1-3 分钟）
3. 部署成功后，你会看到一个 URL，例如：`https://beat-training-2.vercel.app`

#### 步骤 6: 访问你的网站

点击部署后的 URL，你的应用就可以在线上访问了！

---

### 方法二：通过 Vercel CLI

#### 步骤 1: 安装 Vercel CLI

```bash
npm install -g vercel
```

#### 步骤 2: 登录 Vercel

```bash
vercel login
```

会打开浏览器让你登录。

#### 步骤 3: 部署项目

在项目目录中运行：

```bash
vercel
```

首次部署会询问一些问题：
- **Set up and deploy?** → 输入 `Y`
- **Which scope?** → 选择你的账号
- **Link to existing project?** → 输入 `N` (首次部署)
- **What's your project's name?** → 输入项目名或直接回车
- **In which directory is your code located?** → 直接回车 (当前目录)

#### 步骤 4: 生产环境部署

预览部署成功后，运行：

```bash
vercel --prod
```

这会部署到生产环境。

---

## 后续更新

### 更新代码并重新部署

每次更新代码后：

#### 1. 提交更改到 Git

```bash
git add .
git commit -m "描述你的更改"
```

#### 2. 推送到 GitHub

```bash
git push
```

#### 3. 自动部署

如果使用 Vercel 网站方法，**推送后会自动触发部署**！

你可以在 Vercel 仪表板中查看部署状态。

---

## 常用 Git 命令速查

```bash
# 查看状态
git status

# 添加文件
git add .                    # 添加所有文件
git add 文件名                # 添加特定文件

# 提交
git commit -m "提交信息"

# 推送
git push                     # 推送到远程仓库
git push -u origin main      # 首次推送并设置上游

# 拉取更新
git pull                     # 从远程拉取最新代码

# 查看提交历史
git log

# 查看远程仓库
git remote -v
```

---

## 常见问题

### Q: 构建失败怎么办？

A: 
1. 在 Vercel 仪表板查看构建日志
2. 确保本地可以成功构建：`npm run build`
3. 检查是否有环境变量缺失

### Q: 如何自定义域名？

A: 
1. 在 Vercel 项目设置中
2. 进入 "Domains" 部分
3. 添加你的域名并按照提示配置 DNS

### Q: 如何回滚到之前的版本？

A: 
1. 在 Vercel 仪表板中
2. 找到之前的部署
3. 点击 "..." → "Promote to Production"

### Q: 如何查看部署日志？

A: 
1. 在 Vercel 仪表板中
2. 点击你的项目
3. 选择具体的部署
4. 查看 "Build Logs" 和 "Function Logs"

---

## 完成！🎉

现在你的应用已经：
- ✅ 代码托管在 GitHub
- ✅ 自动部署到 Vercel
- ✅ 可以通过 URL 访问

每次你推送代码到 GitHub，Vercel 会自动重新部署！

---

## 需要帮助？

如果遇到问题：
1. 检查 Vercel 构建日志
2. 确保本地 `npm run build` 成功
3. 查看 [Vercel 文档](https://vercel.com/docs)
4. 查看 [GitHub 文档](https://docs.github.com)
